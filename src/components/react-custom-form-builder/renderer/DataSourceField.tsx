// DataSource was dropped from the core — a Widget covers the same use-case
// (async options + search + debounce) with one concept instead of two.
// This file is intentionally an inert stub; see the async Widget example in
// the docs for the replacement pattern.
export {};
